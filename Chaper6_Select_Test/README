/*lingtao Relax it in 2015/04*/
In this part you can test how to use 'select' to make the I/O multiple, and make the server
concurrent.
Instead of using mulitiple threads which will consume much OS resource, select is a good choice.
You can make it in naive method with gcc.
